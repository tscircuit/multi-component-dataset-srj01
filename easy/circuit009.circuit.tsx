import type { ReactElement } from "react"
import { TopologyPair } from "./_shared"

export default (): ReactElement => (
  <TopologyPair
    name="circuit009"
    topKind="qfp_thermalpad"
    bottomKind="soic"
  />
)
