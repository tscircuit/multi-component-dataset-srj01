import type { ReactElement } from "react"
import { TopologyPair } from "./_shared"

export default (): ReactElement => (
  <TopologyPair
    name="circuit006"
    topKind="qfp"
    bottomKind="qfp_thermalpad"
  />
)
